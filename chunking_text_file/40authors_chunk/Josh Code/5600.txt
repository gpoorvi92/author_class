      {
          temp = p1;
          p1 = p2;
          p2 = temp;
      }

        // render top part of triangle
    //  renderTriangleTop(p1,p2,);

        // render bottom part of triangle
