        if(suffixes != null)
        {
            for(int i = 0; i < suffixes.length; i++)
            {
                if(i > 0)
                    buffer.append(",");
                buffer.append(suffixes[i]);
            }

        }
