        if(prefixes != null)
        {
            for(int i = 0; i < prefixes.length; i++)
            {
                if(i > 0)
                    buffer.append(",");
                buffer.append(prefixes[i]);
            }

        }
