package abstractClasses;

/* 
 * 下面的Person类使用了abstract关键字进行修饰，
 * 表示这是一个“抽象类”；
 * 下面具体说说abstract的一些用法：
 * 1. 修饰类：
 *        abstract修饰类的时候，表示这个类是一个“抽象类”，
 *        这个类不能生成实例对象，
 *        但是可以作为对象变量声明的类型，
