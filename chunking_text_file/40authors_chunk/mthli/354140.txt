 *        也就是编译时的类型；
 *        抽象类就相当于一个半成品，
 *        需要子类继承并覆盖其中的抽象方法
 * 2. 修饰方法：
 *        abstract修饰方法的时候，会使这个方法变成“抽象方法”，
 *        也就是只有声明（定义）而没有实现，
 *        它必须在子类继承实现（覆盖），
 *        除非子类本身也是抽象类
 * 
 * 需要注意几点：
