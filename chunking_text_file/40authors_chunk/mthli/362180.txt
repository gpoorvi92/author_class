     *     仅当在编写一个类没有提供构造器时候，
     *     系统会自动提供一个无参数的默认的构造器，
     *     这个构造器将所有的实例域都设置为默认值（0、false或者null）。
     *     当类中提供了至少一个构造器，
     *     但是没有提供无参数的构造器的时候，
     *     则在构造对象的时候如果没有提供相应的参数则被视为不合法。
     */
    public Emplogee() {
    }

