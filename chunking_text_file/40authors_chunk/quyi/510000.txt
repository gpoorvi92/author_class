  }

  public void setFieldValue(_Fields field, Object value) {
    switch (field) {
    case CHECK_TIME:
      if (value == null) {
        unsetCheckTime();
      } else {
        setCheckTime((String)value);
      }
