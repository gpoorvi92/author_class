        if (uploadStateListener != null) {
            uploadStateListener.onFileUploadCancelled(taskID);
        }
        uploadStateListener.onFileUploadCancelled(taskID);
    }

    public String getDir() {
        return dir;
    }

