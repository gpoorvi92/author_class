
        SurfaceHolder holder = mSurfaceHolder;
        if (holder != null)
            itemPlayer.setDisplay(holder);

        return itemPlayer;
    }

    @Override
    public int getCurrentPosition() {
